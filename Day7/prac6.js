// Task: Use map to double the numbers in an array.
let arr = [1,2,3,4,5];
let upd = arr.map(num=>num*2);
console.log(upd)
//Challenge: Use filter to get number greater than three
let fil = upd.filter(num => num>3);
console.log(fil)